import random
import string

def gerar_senha(tamanho):
    senha = ''.join(random.choice(string.digits) for _ in range(tamanho))
    return senha

tamanho_senha = int(input("Digite o tamanho da senha desejada: "))

print("Senha gerada:", gerar_senha(tamanho_senha))
